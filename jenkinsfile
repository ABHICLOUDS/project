pipeline {
    agent any
    stages {
        stage('Pull'){
            steps {
                
                git 'https://github.com/ABHICLOUDS/terraform-code.git'
                sh 'ls'
            }
        }
        stage('terraform init'){
            steps {
                sh 'terraform init'
                sh 'terraform plan'
            }
        }
    }
}
